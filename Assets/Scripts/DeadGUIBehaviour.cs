using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeadGUIBehaviour : MonoBehaviour {
	/*
	public TextMesh score;
	private int _score;
	public TextMesh highscore;
	private int _highscore;

	public TextMesh frindName;
	public TextMesh frindscore;
	private int _frindscore;
	public TextMesh programmmerScore;
	private int _programmerScore;

	public PlayerManagerBehaviour player;

	public void SetActiveGUI(bool value) {

		Debug.Log ("active" + value);

		gameObject.SetActive (value);
		_score = -1;
		_highscore = -1;
		_frindscore = -1;
		_programmerScore = -1;

	}

	void Update () {

		if (player == null) {
			return;
		}

		if (player.score > _score) {

			_score += Mathf.CeilToInt ((player.score - _score) * .1f);
			score.text = _score.ToString ("000000000");

		}

		if (player.highscore > _highscore) {

			_highscore += Mathf.CeilToInt ((player.highscore - _highscore) * .1f);
			highscore.text = _highscore.ToString ("000000000");

		}

		if (player.friend > _frindscore) {

			_frindscore += Mathf.CeilToInt ((player.friend - _frindscore) * .1f);
			frindscore.text = _frindscore.ToString ("00000000") + "x";

		}

		if (player.programmer > _programmerScore) {

			_programmerScore += Mathf.CeilToInt ((player.programmer - _programmerScore) * .1f);
			programmmerScore.text = _programmerScore.ToString ("00000000") + "x";

		}

	}
*/

}
